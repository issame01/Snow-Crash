When We logged into level02, we found a usefull file inside our the home directory which is level02.pcap

So we’re dealing with

File: level02.pcap
Goal: extract credentials from captured network traffic
Target user: flag02
After login → run getflag → password for level03

This means: plaintext credentials leaked over the network